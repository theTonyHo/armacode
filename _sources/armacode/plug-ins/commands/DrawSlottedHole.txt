.. index:: DrawSlottedHole (Command)

.. _drawslottedhole_cmd:

DrawSlottedHole
---------------
Add a slotted hole into the document.

Prompts the user to define the radius and draw a line for slotted hole.

Update 31.03.2015:
    Added Dynamic Draw