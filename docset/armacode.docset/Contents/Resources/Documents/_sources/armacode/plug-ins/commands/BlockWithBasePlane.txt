.. index:: BlockWithBasePlane (Command)

.. _blockwithbaseplane_cmd:

BlockWithBasePlane
------------------
Extends builtin `Block` command.
Create a block definition with specified plane. User will be prompted to select a surface as base plane.
Base Plane is defined by the surface's evaluated plane at normalised param uv (0.5,0.5)