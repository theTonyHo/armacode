.. index:: NextSelectedObject (Command)

.. _nextselectedobject_cmd:

NextSelectedObject
------------------
Loop through the selected objects and zoom in each one by one.
This is useful for visually inspecting a set of objects.
The order of the selected objects is assumed to be sorted by the order of creation.